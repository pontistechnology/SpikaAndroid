package com.clover.studio.exampleapp.utils.extendables

import android.app.AlertDialog
import android.content.Context

open class BaseDialog(context: Context?, themeResId: Int) : AlertDialog(context, themeResId)